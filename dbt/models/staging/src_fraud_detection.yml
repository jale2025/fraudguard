version: 2

sources:
  - name: src_fraud_detection # table_name -> dbt_<target>_stg.src_fraud_detection
    schema: raw
    tables:
      - name: transactions
      - name: labeled_predictions_queue
      - name: predictions 
